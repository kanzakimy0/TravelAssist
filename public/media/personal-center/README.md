# Personal Center runtime media

Owned by B / Personal Center.

## WBS 5.1

- `sidebar-torii-watercolor.svg` — decorative Sidebar artwork.

Use the SVG inside the existing cropped Sidebar artwork container. It is decorative and should use empty alt text / `aria-hidden` semantics.

Do not add navigation icons, buttons, focus states or avatar UI as raster assets; those remain code-driven.
