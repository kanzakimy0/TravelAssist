# Detail 行程卡操作与移动名称框 Result

## Status

UI 与本地处理逻辑完成，待用户验收；Mapbox 连接待令牌来源授权。本轮没有提交、上传或合并。

## Tracking

- Base：`540e6dbaae8512084d08e308396c8999e1ba5a77`。
- Branch：`codex/planner-sight-timeline`。
- 保留前一轮紧凑景点时间轴改动，见 `RESULT-planner-sight-timeline.md`。
- WBS 为本地补修记录，不更改已合并的正式 Task / Issue / PR 状态。

## Implemented

- 有明确且可安全执行的时间建议时，“同意”直接调整该项目，保留持续时间和其他项目；重新计算本地检查状态。没有安全建议时显示“更改”，进入项目详情，不能以确认按钮伪造预约完成。
- 已预约、已购票、预约处理中、固定时间及锁定项目保留保护，不自动修改；无可用时间时不覆盖其他行程。
- “无视”隐藏当前提醒格，后续预约卡补上；不删除项目，也不把实际错误改成正常。按方案、项目及当前问题内容记录，内容变化后会重新提醒，沿用浏览器草稿保存流程。
- 未安排的餐饮和住宿显示居中大加号；下面等宽“更改 / 无视”。无视仅隐藏下面提醒，顶部加号继续可用。
- 提醒显示具体建议时间，主按钮突出、次按钮轻量；问题节点的红色感叹号保留并复验。
- 移动页景点名称框加高 30%，保持原宽度和字体，不改交通编辑业务或底栏高度。

## Validation

- lint、typecheck、production build、git diff --check：通过。
- 全部 Node tests：356 通过，新增 9 项覆盖实际时间变更、保护、忽略与重新提醒、空白项、无空档及草稿恢复。
- 相关历史 QA 脚本同步新的按钮名称和名称框比例；本轮未独立运行这些脚本。
- 本轮修改文件格式检查通过。全仓已知的 31 份原有文档格式问题不在本轮范围。

## Browser QA

- 生产预览 3113 已重建并重启；检查 1280×720、1440×900 和 390×844。
- 浅草寺“同意”后由 10:00 调整至 10:15，结束时间同步为 11:45；提醒消失，不意外打开项目详情框。
- 午餐“无视”后项目保留，预约卡补位。
- 晚餐加号打开项目详情与餐厅推荐；60px 卡中的加号区域约 34px 高，不被额外说明挤压。
- 红色冲突示例实际显示“!”与红色节点；测试未保存，不覆盖用户已保存行程。
- 移动名称框实测 44×57.2px，符合 1.3 倍高度。
- 手机抽屉内加号和更改 / 无视按钮可见；未见 console / hydration 错误。测试页面已关闭，窗口覆盖已重置。

## Mapbox / Limitations

- 当前预览缺少本地 Mapbox Token，仍使用可操作 fallback，不宣称已连通真实底图。
- 安全检查阻止从旧 `TravelAssist 2/.env.local` 复制公开令牌，已请求用户明确授权该来源；没有写入当前预览配置，没有提交令牌。
- 获得授权后仍需本地配置、重建及真实 Mapbox 浏览器验证。
- 时间调整仅为本地结构检查，不是实时交通计算、营业时间查询、真实 AI 或预约服务。
