# Planner 酒店端点与范围面板 Result

## Status

2026-09-07，本地待审查。Branch `codex/planner-sight-timeline`；Base `540e6dbaae8512084d08e308396c8999e1ba5a77`；WBS 4.36；Issue / Commit / PR PENDING。未 commit / push / merge。

## Implemented

- 行程与交通使用同一项目列表、同一排序及名称格式，包含酒店出发、三餐与回酒店，拖拽调整后相邻移动段同步。
- 每个方案首次进入行程 / 交通时一次性补齐缺失的酒店端点；时间只是可编辑草案，未选酒店明确标为占位，不产生真实 POI、费用或订单。不移动已有行程 / 预约；可移回备用，后续切换选项卡不会重新添加；既有浏览器保存兼容初始化标记。
- 预约：只展示所选范围内确需购票 / 预约的景点、活动、交通；剔除无需预约 / 已取消 / 地点占位。保留待处理与已确认的真实状态字段，库存未知保留待办并标明未核对；酒店餐饮具体预约仍在 Detail。
- 备选：单日最多四个户外影响点，三日 / 全日逐日比较，三类调整目标均保留预览入口，全日不再只有说明文案；受保护节点不能被替换。
- 餐宿：单日三餐覆盖与住宿落点，连续日对比缺口，全程城市衔接；区域理由 / 接驳说明与进入单日入口保留，不接新推荐 API。
- 旅行体检：实际草案的重叠 / 无效时间 / 未选地点、已估交通及未知段、衔接风险、餐饮覆盖；不再把项目间空档算作实际交通时长。
- 展开摘要按所选范围汇总，不再所有范围都只显示焦点日；说明文字与预约过滤规则一致。
- 修复素材目录中的雨天分类索引错误：摄影步道不再因数组下标被标为室内；博物馆等明确室内名称不再列为户外天气影响。

## Validation / Boundaries

- Node 全套 372 项通过；新增 6 项验证初始化幂等、端点移回备用 / 保存、项目与移动顺序、范围隔离、预约过滤、体检及室内分类。
- lint / typecheck / build / 改动文件格式 / diff check 通过；全仓既有 31 份文档格式例外保持，不重写无关文件。
- 浏览器真实操作：1440×900 对比行程与交通七项名称顺序完全一致；单日预约 1 项、三日 3 项；全日餐宿 / 备选显示全程范围，保留预览与进入单日；无控制台 error / warn。
- 最终构建复验全日展开摘要：全程 3 日 / 3 城、待核对购票 3 项，与列表一致；390×844 当天安排抽屉中的全日预约列表可滚动，页面宽度 390px，无横向溢出。没有覆盖用户浏览器保存。
- 预览地址 `http://127.0.0.1:3113/planner`，工作树 `I:/Users/kanza/AppData/Local/Temp/TravelAssist-task012`。
- 保持底栏高度、右栏比例、Map 生命周期和 Detail 保存职责；未改 /start 或 Personal Center。
- 天气 / 渠道 / 交通仍为本地示例及用户估计；当前地图用 fallback 验证，没有添加 Token、调用真实 API 或生成预约订单。
