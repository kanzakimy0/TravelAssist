# Planner 景点与备用双层时间轴 Result

## 状态与追踪

- 本地实现完成，待用户视觉验收；未 commit / push / merge。
- 用户来源：本次“行程的景点和备选按照时间轴严格设计，大小不需要太大，和行程卡差不多”补修请求。
- 基线：`540e6dbaae8512084d08e308396c8999e1ba5a77`。
- 分支：`codex/planner-sight-timeline`。
- WBS：1.5 / 4.8 的展示层补修；正式任务状态不变。
- 新 Issue / Commit / PR：未创建；不把本轮改动算入已合并 PR #139 或已关闭 Issue #135。

## 实现

- 抽出 PlannerSightTimeline，隔离移动编辑器和 Detail 的样式。
- 方案景点采用有序列表、实线节点与垂直短连线，节点对准各卡中心。实际起止时间与景点名称放在卡内；按时间先后等宽排布，不把卡宽误称为分钟比例。
- 备用使用独立的虚线和空心节点，明确“待安排 / 待恢复”，不虚构已确认的时间。加入后仍由原有逻辑查找当天空档，无空档则拒绝添加。
- 桌面卡宽 124px、手机卡宽 120px，高度适应现有栏位，不拉成长条。右侧独立上下箭头负责移动，已保护景点显示锁图标，点击主体只查看详情。
- 两层共用一个横向滚动区域，左侧分类标签固定；多日按日期独立分组。长名称保留完整可访问名称与悬停说明。
- 保持 Planner 底栏约 25dvh，未增加页面高度；没有改动推荐方案、移动业务、Detail、浏览器保存、地图生命周期或素材。

## 验证

- lint、typecheck、production build、git diff --check：通过。
- 全部 Node tests：347 通过；新增 4 项实际组件测试覆盖有序节点、无伪造时间的备用轨道、选择/转移/保护与空状态。
- 原 TASK-011 展示层断言更新为检查抽出的组件，未删减旧有保护检查。历史浏览器脚本的按钮选择器同步到新的可访问名称；该脚本本轮未独立运行。
- 修改文件的 Prettier 检查：通过。全仓 format:check 仍有 31 份未改动的基线文档失败，未越界重排。

## 浏览器验收

使用生产预览 `http://127.0.0.1:3113/planner`，通过当前浏览器 UI 操作验收；未写入用户已保存行程。

| 尺寸     | 结果                                                              |
| -------- | ----------------------------------------------------------------- |
| 1280×720 | 底栏 180px；卡片约 124×35px，起止时间和名称不被原先的底部按钮遮挡 |
| 1440×900 | 底栏 225px；卡片约 124×54px，不随屏幕变宽而拉伸                   |
| 1024×768 | 多日独立分组；底栏、横向滚动和折叠侧栏正常                        |
| 390×844  | 沿用“当天安排”抽屉；卡片 120×66px，无页面横向溢出                 |
| 320×740  | 抽屉内横向查看剩余备用；上下两条轨道同步滚动，分类标签不移动      |

- 浅草寺移至备用再恢复，实际恢复至原 10:00–11:30。
- 东京国立博物馆等已保护项目不能通过下移箭头移除。
- 点击计划景点打开项目详情框，再点击备用景点切换同一详情框，不叠窗。
- 没有足够空档时，添加备用仍明确拒绝，不覆盖既有安排。
- 全日三组实际时间按先后排列；手机切换移动页仍显示原 5 个移动段。
- 浏览器检查期间无 console / hydration 错误；窗口测试覆盖已重置。

## 限制

- 当前预览没有 Mapbox Token，验收使用可操作 fallback 地图；没有宣称完成真实 Mapbox 联网验收。
- 未接入真实路线计算、营业时间验证或预约服务；既有本地安排规则保持不变。
- 本轮仅本地交付，GitHub 发布记录待用户要求上传时建立。
