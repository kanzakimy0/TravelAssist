# 方案入口与 Planner 弹层稳定性 Result

## Status

本地实现并验收；`http://127.0.0.1:3113` 预览已更新。
尚未 commit / push / merge。既有未提交的浏览器保存、详情按钮、Step 手机优化改动完整保留；不改变正式 Issue / PR 的状态。

## Changes

1. 方案卡保留同一个按钮：首次点击“查看这个方案”选择；标签变为“进入详细路线”；再次点击先写入现有方案桥接状态，再导航到 `/planner`。不再增加“使用此方案并进入地图”链接；选中前后按钮位置和卡片高度不变。
2. 复现住宿详细设置分区切换时 82px 纵向跳动：原定位算法每次根据内容高度重新贴边。侧向弹层现在保持打开时的顶部基准，只在实际祖先滚动或窗口变化时调整；内容变长使用内部滚动，保留原 top-layer / Escape / 外部点击 / 焦点恢复机制。滚动条预留稳定宽度，避免出现/消失时挤压内容。
3. 复现 390/320px 搜索获得焦点后横向扩张并覆盖 Logo。去掉该扩张规则；小屏保持固定搜索图标，在标题栏下方浮层输入，Escape 关闭，关键词保留。输入字号 16px；仍为原本的搜索入口演示，不接真实搜索 API。

## Validation

- lint / typecheck / build / git diff --check：通过。
- 现有 Node tests：184/184 通过。更新旧导航断言与 QA 入口以匹配用户确认的新单按钮行为。
- 本次改动文件 Prettier 检查通过，不声称全库既有格式异常已修复。
- `tools/qa/planner-stability-check.mjs`：1440×900、1024×768、390×844、320×740；五个快捷设置与三个偏好详细分区共 44 次操作位置比较，通过；四尺寸搜索不覆盖 Logo，输入/关闭/重新打开通过。
- 三方案 × 1440/390/320 三尺寸，共 9 次两次点击导航通过；第一下不离开 `/start`，第二下进入 `/planner`，方案桥接值分别为 classic / depth / relax。
- 原快捷设置回归：1440/1280/1024/390/320 五尺寸 × 普通/减少动效，五快捷卡、三嵌套详情、日期保护、编辑、Escape、外部关闭及焦点恢复均通过，pageerror 为 0；证据 `docs/qa/planner-stability/menu-regression/`。
- 修改前证据 `docs/qa/planner-stability/before/`；最终生产验证 `docs/qa/planner-stability/production/`；开发检查 `after/` 不代替生产结果。

## Non-goals

不修改方案数据、地图生命周期、保存逻辑或推荐方案栏结构；不接数据库、AI、搜索 API；不自动合并。
