# TASK-PLANNER-TRACK-A — 独立行程滑轨与推荐方案操作

## Metadata

- Task ID: TASK-PLANNER-TRACK-A
- Owner: A
- Status: 待验收（用户已授权上传及合并）
- WBS: 4.46
- GitHub Issue: #210
- Branch: `codex/planner-independent-tracks-plan-actions`
- Depends On: 已验收 UI，PR #204 / #206；本次用户明确授权整合合并
- Base: `a2a537bbed9b153b3a71fbb955e8ac23ef30213b`
- Commit: `5879444`（最新实现，整合另记）
- Pull Request: PENDING（未上传）

## 用户本轮要求

### 最新追加：三餐与渠道选择（2026-09-08）

早餐、午餐、晚餐分别按所在路线生成区域建议；保留三餐身份并对超出各自建议时段的安排发出非阻断警告。拖动保留五分钟刻度，浮层只显示落点时间；上下轨分别提示移入备用/行程。修复切换工作方案勾选对齐。预约当日待办支持逐项目手选渠道高亮，未手选默认最低可比较示例价；更换渠道重新核对3秒，演示进度使用所选渠道，不产生真实订单。

### 追加修订：五分钟拖动与工作方案（2026-09-08）

本节覆盖下面旧版对应交互：重叠卡可选择置顶后拖动；直接拖动按五分钟吸附并实时预览落点；时间菜单保留精确填写及原相邻插入。餐宿四卡各显示多个行程周边区域，悬停/聚焦/点击显示理由，三日每卡分为三层。

所有首次详情入口必须确认保存；保存后成为当前工作中方案，返回推荐后仍保留身份，重新生成不得覆盖。其他方案按钮改为切换方案，切换明确警告废弃原工作状态，默认建议先另存浏览器草稿；可恢复草稿，存储失败阻止切换。左右操作按钮等宽。

本次仅本地继续，未获得上一轮所请求的 GitHub 发布授权，Issue/PR 仍 PENDING。

追加实现提交：`5c276e268c1f8f75f42dcfa71e54290da8de6e43`；544项测试及四尺寸验收通过。

1. 修复拖动撑长时间条；上下轨独立横向滚动。
2. 三日模式去掉左侧两块标签，日期与第几天置于两轨正中。
3. 解释锁定，保留预约、固定时间、跨日保护及手动解锁。
4. 单日餐宿分为早餐、午餐、晚餐、住宿四卡，各含区域与理由。
5. 推荐方案新增显式保存到浏览器并进入详情、确认后还原推荐，修改后红色标识。
6. 详情返回改为“返回推荐及增删项目”。

## 边界与验收

保持底栏高度、地图生命周期、其他方案、独立详情项目与原工作区。保存仅当前浏览器，无真实预约、云同步或自动保存。还原不取消外部订单。

本地实现沿用用户正在预览的最新 UI，不切回缺少这些改动的旧 UI 基线。后续发布必须保留 PR #204/#206，并与最新 develop 追踪文件安全协调，不能用本分支历史 WBS 回退数据库进度。

验收见 `RESULT-planner-track-actions.md`。GitHub Issue 创建被权限检查拒绝，不换其他渠道绕过；待用户明确授权发布代码及任务记录后再 push / Draft PR。
