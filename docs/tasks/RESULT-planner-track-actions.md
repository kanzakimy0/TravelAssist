# TASK-PLANNER-TRACK-A Result

## 最新追加：三餐时段、拖动文案与预约渠道（2026-09-08）

- Status：本地实现完成、待用户验收；未上传或合并。以下规则覆盖旧记录的对应文案。
- 早餐按住宿出发/首站、午餐按午间附近路线、晚餐按后段/回酒店方向分别生成区域推荐与理由，仅复用已有实体，不查询真实餐厅。
- 本地建议开始时段：早餐06:00–10:30、午餐11:00–14:30、晚餐17:00–21:00；超出时提示，不禁止拖动，也不冒充营业时间。修改时间、替换餐厅、预约调整后保留三餐身份；总览和详情不因移时把早餐误认成午餐。时间冲突仍优先显示红色。
- 拖动浮层仅显示落点时间，例如11时55分；保留五分钟吸附。上轨提示“拖到下轨，移入备用”，下轨提示“拖到上轨移入行程”。
- 工作方案切换确认的勾选框固定16px，文字独立列，窄屏换行不挤压或错位，确认按钮仍等宽。
- 预约核对每个项目可手选渠道、高亮；再次点击取消手选，恢复已有可比较JPY示例报价中最低价。无报价仍待查询。选择变更重置3秒核对时间，提交演示进度保留所选渠道；原行程变更失效保护、零真实订单边界不变。
- Validation：548项测试全部通过；lint、typecheck、build、diff检查通过。全库format仍有28个未修改基线文档问题，本轮修改文件已格式化。
- Chrome：1440×900、1280×800、390×844、320×740工作方案/重叠拖动回归通过；1440×900、390×844、320×740渠道手选、取消、自动最低价、3秒倒计时、进度渠道一致通过，无pageerror。审阅了手机渠道和320px勾选截图。
- QA：新增 `tools/qa/planner-booking-choice-check.mjs`；更新 `tools/qa/planner-working-plan-check.mjs`。证据仅本地 `.cache/qa/`，未提交浏览器数据或Token。
- 本地预览：http://127.0.0.1:3113/planner。Issue/PR仍PENDING，不接实时比价或真实订单，不自动merge。

## 最新追加验收：五分钟刻度与工作方案

- Status: 本地完成、待用户验收；未上传。追加实现 Commit: `5c276e268c1f8f75f42dcfa71e54290da8de6e43`。
- 重叠判断覆盖相近时间的视觉碰撞，不只判断完全相同开始时间。提供列表选取置顶，自动滚动到该卡并聚焦拖拽入口，受保护项目仍不可移动。
- 指针映射时间轴坐标并按五分钟吸附；浮层实时显示“预计放下：11时55分”，实际落点与显示一致。键盘左右每次五分钟，上下换轨，Escape取消。保留项目时长和其他项目时间，移动摘要按新相邻关系失效；原菜单精确填写/结束后15分钟插入保留。
- 早餐/午餐/晚餐/住宿四卡各有2–3个示例区域：现有行程景点周边加已有区域数据，不伪造坐标或Provider ID。Hover/Focus及手机点击显示理由；3日每卡内部三等分，日期与区域同排，无额外常驻理由卡。
- 主按钮、方案按钮、Capabilities入口和直接详情URL均走首次保存确认。工作身份写入可校验浏览器快照，刷新恢复，老版本保存记录兼容读取。
- 当前方案显示“当前工作中方案”和“进入行程详情”；其他方案显示“切换方案”。重新生成现有Mock逻辑保留整个工作方案，不真实调用路线API。
- 切换默认建议先保存旧工作为草稿，另需确认替换当前保存记录。失败不切换、不丢弃；最多50份，不自动删除。当前保存成功后才切换身份；旧工作路线恢复为原始推荐，旧独立详情草稿随归档保存，切换的新工作不串入旧详情项目。
- “浏览器草稿”可恢复；恢复也经过切换确认。重复保存同一草稿是no-op。损坏目录、无效身份、配额异常、保存记录被另一页修改时阻止破坏性覆盖。
- 新增8项回归，总计544项通过；lint/typecheck/build/diff检查通过。全库格式检查仍有28个未修改基线文档问题，本轮无新增。
- Chrome四尺寸：1440×900、1280×800、390×844、320×740。验证重叠拖拽到11:55、三日12层区域行、首次详情取消及保存、按钮等宽、工作身份、切换取消、配额失败保护、切换归档、刷新及恢复草稿；无pageerror。截图检查修正了三日区域选项垂直压缩问题。
- 新验收脚本：`tools/qa/planner-working-plan-check.mjs`，取代上一版本交互验收脚本；截图/报告仅本地`.cache/qa/planner-working-plan/`。不修改用户已有浏览器草稿。
- 新模型：`working-drafts.ts`与`meal-area-choices.ts`。不接云端草稿、酒店餐厅查询、实时路线或真实预约；工作方案仅本机浏览器状态。

以下为上一版验收记录，旧的按钮与显示规则以本节及最新Task为准。

## Status / Tracking

- 本地实现完成、待验收；GitHub 发布受阻，不宣称已上传或合并。
- Branch: `codex/planner-independent-tracks-plan-actions`
- Base: `a2a537bbed9b153b3a71fbb955e8ac23ef30213b`，保留 PR #204 / #206 最新 UI。
- Implementation Commit: `83cf4687eb2ddc84583c30fd86cb8605b935a836`（后续提交仅同步记录）
- Issue / Draft PR: PENDING，创建 Issue 被权限检查拦截，等待用户授权发布本次代码及任务元数据。
- WBS: 4.46 待审查（本地）；原工作区未切换或覆盖。

## Implemented

- 拖拽浮层移出有 containment 的轨道；手机在所属 dialog 顶层显示。时间尺保留真实比例，但不再按最小时间间隔无限扩宽，上限 1440px。
- 上下两个 viewport 分别滚动，边缘拖拽只滚动鼠标所在轨道，备用项目不随上轨移动。
- 三日模式去掉左侧标签，日期与第几天居中分隔上下轨；手机按日纵向排列，各日轨道仍独立。
- 已预约/出票、固定时间、跨日项目保留保护，点击锁按钮可查看原因；普通手动锁支持再次解除。
- 单日四张餐宿区域卡，区域选择、内嵌理由和详情入口，不再有独立“为什么推荐这里”卡。区域是现有示例数据，选择仅预览，不冒充真实餐厅酒店预订。
- 每个推荐方案新增“保存并细化 →”“还原推荐”。保存弹窗明确浏览器单份存储与覆盖确认，成功后进入所选方案详情；未保存成功不跳转。
- 已修改方案显示红色“已修改”。还原需确认，只恢复目标方案原始 Mock 推荐路线、名称、时间、备用与移动修改，其他方案和独立详情草稿保留，不覆盖已保存版本、不取消外部订单。
- 返回按钮改为“返回推荐及增删项目”，原未保存保护保留。

## Validation

- 536 项 Node tests 全部通过。新增 4 项时间轴宽度、初始修改状态、定向还原与无效目标测试。
- 原 TASK-012 的完全冻结结构断言按用户本轮明确授权更新；保留图片、方案顺序、名称摘要与选择行为检查。
- lint、typecheck、build、git diff --check 通过。
- 全库 format:check 存在 28 个未改动基线文档问题；本轮修改文件格式通过，不宣称全库通过。
- Chrome 152，1440×900、1280×800、390×844、320×740：拖拽不撑宽、两轨独立、三日分隔、四餐宿卡、还原取消/确认、非当前方案保存进入详情通过；无 pageerror / 页面横向溢出。
- 1440 使用真实 Mapbox；手机强制 fallback。使用独立浏览器上下文，不改动用户已有草稿。
- QA: `tools/qa/planner-track-actions-check.mjs`；截图与报告位于本地 `.cache/qa/planner-track-actions/`，不提交生成图片、Token 或浏览器存储。

## Limitations / Follow-up

- 保存仅浏览器，目前一份；非账号云保存。
- “还原推荐”目前基于三套现有 Mock 推荐，后续真实生成方案需要保存不可变原始版本。
- 时间重叠仍允许，详情负责可行性判断；不接路线 API。
- 待授权 GitHub 发布并与 develop 协调父 PR；不自动 merge，不开始后续任务。
