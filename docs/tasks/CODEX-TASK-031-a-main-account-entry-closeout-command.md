# Codex Command — TASK-031-A

Run only after TASK-030's chosen base is available; do not develop both against the same stale Home files in parallel.

Create `codex/a-main-account-entry-closeout` from the latest safe `origin/develop`. Read TASK-031, latest WBS, Auth User Flow Task/Result, Header/Home source and Personal Center navigation contract.

Audit first. Reuse B Auth/session code; A owns only the main-system entry/presentation boundary.

Validate signed-out, signed-in, loading/error, keyboard/focus, desktop/mobile, and Home/Start/Planner/Detail regressions. Run full tests/lint/typecheck/build.

Update Result/WBS 3.4/Issue #261 and create a Draft PR. Do not merge. Stop before TASK-033.
