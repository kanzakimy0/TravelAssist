# TASK-055-B gap audit

Baseline: `3850ec91a94b96f61800a9e45ba7792d07bb1c44` (clean execution-time `origin/develop`). The audit starts with repository test files, existing npm entry points, accepted Task/Result/QA evidence, and current handler/schema contracts. `test-inventory.json` records 62 suite/script files and 15 support files, including files with no npm alias. `coverage-matrix.json` maps 46 critical 9.5 behaviors to executable assertions; it is not an instrumented line-coverage percentage.

## Focused gaps closed

| Gap                                              | Why existing evidence was insufficient                                                                                                                                                                                  | Added assertion                                                                                                                                                                                                                                        |
| ------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Live Auth outage across private handlers         | Existing real Auth suites prove invalid credentials, ownership and deleted users. Those cases do not prove a transport/service failure remains fail-closed before product DB/Admin I/O.                                 | Five real private handlers receive a controlled `/auth/v1/user` 503. Each must return deterministic `AUTH_UNAVAILABLE`, private/no-store headers and `Vary`, after exactly one Auth request and no product DB/Admin request.                           |
| Profile second live-verification boundary        | Profile projects Auth contact truth after the shared authorization check. A previously successful first verification alone does not prove the second check cannot change identity or become unavailable before a write. | Valid PATCH after first user A: second user B gives `AUTH_REQUIRED`; second Auth 401 or 503 gives the accepted `AUTH_UNAVAILABLE` shape. No product persistence request occurs. This records the current accepted error contract, without changing it. |
| Multibyte streamed request body                  | Existing parser tests cover malformed input/size checks but do not prove valid UTF-8 split one byte per chunk is accepted at the exact byte ceiling.                                                                    | Unicode JSON split across all multibyte boundaries succeeds at the true byte limit, fails at one byte less despite a false Content-Length, and releases its reader on both paths.                                                                      |
| Aggregate can silently report incomplete success | A new orchestration boundary must preserve real child failures and reject mandatory skips or missing TAP, including Node nested-test reporter behavior.                                                                 | Four real Node subprocess probes: pass, assertion failure, zero-exit skipped test, zero-exit no-test output. No second framework or product test duplication.                                                                                          |

There are 9 new product-boundary assertions and 4 runner assertions (13 total). Controlled Auth transport responses are appropriate for deterministic failure injection; real Local Auth, Cookie/Bearer, cross-user RLS and deletion are separately rerun by the Local aggregate. No production defect requiring a code change was found. There is no business-code, schema, migration, generated-type or dependency change.

## Reused accepted coverage

The canonical non-Local selection runs 28 files. It preserves existing focused aliases and reuses Profile/Auth tests without aliases, canonical schema/parser/API tests, Personal Center domain/view-model tests and the narrow WBS 9.12 unit baseline. Historical view-model defaults are presentation fixtures; they do not override the sparse persisted Preference contract. TASK-042/045/046/053 remain authoritative for unset/save/reset/preset behavior.

The Local selection runs 13 real integration suites plus 2 existing client-bundle audits, in sequence. It covers Profile DB and Auth core, Preference schema/API/read contract/presets, Companion schema/API, Trip schema/API, Profile API, deletion and Migration integration. Two distinct synthetic users plus anonymous requests prove isolation. Existing browser use is limited to accepted single-feature API/Auth boundaries; it does not introduce a new browser journey. The inventory lists actual commands, constraints and cleanup for every selection.

TASK-054's accepted double replay is referenced rather than destructively repeated: its six migration checksums, two real replay reports and catalog/type evidence are already accepted in `docs/qa/TASK-054/`. TASK-055 independently reruns the immutable migration contract, real catalog/RLS/cascade tests and Local type generation on both passes. It neither edits history nor adds a migration.

## Exclusions and WBS 9.6

Nineteen historical scripts are outside the aggregate: sixteen standalone browser/visual harnesses, two legacy full Auth-flow runtime harnesses, and the destructive TASK-054 replay driver. Every exclusion has an individual reason in the inventory. Excluded scripts are inventoried, not claimed as newly passed. Existing visual/responsive scope accepted under 9.12 is not reopened. Legacy WSL-specific full Auth-flow harnesses are not rewritten merely to add test counts; current TASK-018 runs the real Auth technical contracts under 9.5.

Only four intrinsically full-journey behaviors are deferred. `coverage-matrix.json` gives the reason and expected assertion for each: complete registration/login/multiple pages/logout/relogin; cross-module browser persistence; complete desktop/mobile navigation; and the full browser account lifecycle through deletion. Unit/API evidence cannot prove the continuity of these multi-page journeys. No uncovered critical 9.5 behavior is deferred to documentation, and WBS 9.6 remains unstarted.

## Baseline debt

The baseline full repository suite passed 2,480 tests. Baseline `npm run lint` reports seven `@typescript-eslint/no-require-imports` errors exclusively in ignored `.cache/qa/task024-worktree/.cache/qa/*.cjs`. Candidate lint must reproduce the exact same output, while lint excluding that ignored cache and the clean exact-head GitHub Quality Gate must pass. The final evidence records hashes and results; these errors are not a TASK-055 exemption for changed files.
